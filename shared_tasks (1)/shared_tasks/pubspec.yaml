name: shared_tasks
description: Shared daily tasks for two people (Arabic RTL, realtime).
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  supabase_flutter: ^2.5.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
